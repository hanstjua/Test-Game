using System.Collections;
using System.Collections.Generic;
using Battle;
using UnityEngine;

public class AttackAnimationParameters : MonoBehaviour
{
    public float damageNumberScaler;
    public float textPositionScaler;
    public float textAlphaScaler;
    public float actorAnimationTrigger;
    public float targetAnimationTrigger;

    public BattleProperties battleProperties;
    public HpDamage hpDamage;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
