using System.Collections;
using System.Collections.Generic;
using Battle;
using UnityEngine;

public class PoisonAnimationParameters : MonoBehaviour
{
    public float damageNumberScaler;
    public float textPositionScaler;
    public float textAlphaScaler;
    public HpDamage hpDamage;
    public BattleProperties battleProperties;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
